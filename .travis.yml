sudo: false
language: node_js
node_js:
  - '10'

notifications:
  email: false

before_install:
  - mkdir www/ && mkdir www/assets/

script:
  - npm install firebase-functions firebase-admin firebase-functions-test
  - npm run build
  - npm run test
  - npm run deploy

after_script:
  - npm run pagespeed
